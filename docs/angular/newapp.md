# Gerando uma nova aplicação (Angular CLI)
* Para gerar uma nova aplicação no angular cli:
```ts
  ng new nome-aplicacao --prefix=exemplo
```
!!! warning "Atenção"
    **--prefix=** é o prefixo dos componentes que serão gerados na aplicação
